var five = require("johnny-five"),
  board, slider, servo, range;

board = new five.Board();

board.on("ready", function() {
	//sensores = new five.sensores.Array([8, 12, 13]);
	//sensor=new five.sensor({
	//	pin:8
	//});
	//var sensor=this.pinMode(8, five.Pin.INPUT);
	led = new five.Led({
    	pin: 13
  		});
  	servo = new five.Servo({
    	pin: 6,
    });
	 s1=this.pinMode(3, five.Pin.INPUT);
   s2=this.pinMode(4, five.Pin.INPUT);
   s3=this.pinMode(5, five.Pin.INPUT);
   s4=this.pinMode(6, five.Pin.INPUT);
  	this.digitalRead(s1, function(value) {
    	console.log(value);
    	if (value==1) {
    		servo.to(90);
    	} else{
    		servo.to(0);
    	};
  });
});